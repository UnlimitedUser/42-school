var a = Color.hearts

print(a, "-> a.rawValue =", a.rawValue)

for c in Color.allColor {
	print(c, "-> c.rawValue =", c.rawValue)
}

var b = Value.ace

print(b, "-> b.rawValue =", b.rawValue)

for v in Value.allValue {
	print(v, "-> v.rawValue =", v.rawValue)
}
